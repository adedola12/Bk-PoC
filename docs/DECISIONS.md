# BK-Ingest — Decisions Log (continues from D1–D11 in the KB and Addendum)

| ID | Date | Decision | Implication |
|----|------|----------|-------------|
| D12 | 18-Jul-2026 | **Triage label set gains a sixth structural route: `bk_template`.** Files whose workbook matches the BK template signature (sheet set Template/Lookup/Instructions, or a Template sheet whose header row matches the known BK column set) route to `bk_template` — detected by deterministic rule before any LLM call. Filled templates (the Twyford golden file) short-circuit to validation per Design Doc §3 Stage 1. | T1 ground truth: template + Twyford → `bk_template`; 3 datasheets → `product_datasheet`; 2 Bosch → `catalogue`; 2 JPGs → `product_image`; SG_BK.docx → `vendor_document`. D11 verification prompt remains required for `vendor_document`/`other` only. Ratified by ADLM 18-Jul-2026. |

| D13 | 18-Jul-2026 | **Repo restructured to the ADLM house layout**, amending BUILD_PROMPT §4: `client/` + `server/` at root (no npm workspaces), `server/index.js` + `server/db.js` entry, PascalCase named-export models, `{ error }` API error shape, taxonomy/emitter as `server/taxonomy` + `server/emitter`. Client: `config.js` env centralization, `createBrowserRouter`, `React.*` hooks, Plus Jakarta Sans + flat design system, framer-motion. | Matches the engineer's review workflow (ADLMWebsite conventions); deploy targets: client→Vercel, server→Render. Stage/pipeline module layout under `server/` is otherwise unchanged. Ratified by ADLM 18-Jul-2026. |

| D14 (ratified 18-Jul-2026) | 18-Jul-2026 | **Classifier-disagreement precedence:** label precedence deterministic > lexical > semantic, EXCEPT when two methods agree against the third — the majority node wins. Confidence: all agree 0.95 · two agree 0.8 · lexical-only 0.7 · semantic-only 0.6 · full conflict 0.4 + REVIEW flag. | Implemented in `server/stages/stage4_classify.js`; agreement raises confidence per D1, disagreement routes to review. |
| D15 (ratified 18-Jul-2026) | 18-Jul-2026 | **Semantic classification tier runs as Claude semantic matching for the PoC** (the D8 stack is Anthropic-only; no embeddings endpoint). A local sentence-embedding model is the productionization path per Design Doc §4. | Cost order preserved: deterministic → lexical → semantic-LLM. |

| D16 (ratified 18-Jul-2026, requested by ADLM) | 18-Jul-2026 | **URL ingestion:** catalogs may enter via a manufacturer/product weblink as well as file upload. The system fetches the URL (http/https only, private/loopback addresses refused); file responses (PDF/xlsx/docx/image) enter the standard Stage 0 triage flow unchanged; HTML pages are text-extracted and treated as a catalogue text source. | New `POST /api/uploads/url` + URL tab on the Upload page. Provenance: `sourceUrl` recorded on the Upload Ledger entry. |
| D17 (proposed — ratify at final review) | 18-Jul-2026 | **Category-profile exemptions for off-category emission:** the PoC holds only the Wash Basins template (the one BK provided). Products from other categories (accessories, tools) emit through it via per-category mapping profiles; required columns that are structurally inapplicable to the product's category (e.g. Tap Holes for a towel rail) are exempted from the PASS gate and recorded in the run report as pending BK's real per-category template (D6). | Keeps T3 honest: zero-touch is computed over applicable required fields; exemptions are visible, never silent. |

| D18 (proposed — ratify at final review) | 18-Jul-2026 | **Derived-field gate threshold:** fields with method `derived` (drawing-read dimensions, substituted finish codes) pass the Stage 9 gate at ≥ 0.6 instead of 0.85, BECAUSE they remain visibly labelled derived in the IPR, UI and emission — mirroring D5, where derived_unverified SKUs emit labelled rather than blocked. Thresholds stay configurable in `services/confidence.js` (§6.5). | Without this, clean accessories whose dimensions exist only in technical drawings can never auto-pass, defeating T3 for reasons the design already handles by labelling. |

Open: **D19 candidate** — cross-file/re-upload SKU reconciliation: same part number in two uploaded documents = confirmation signal or two candidate rows? Includes the re-upload identity path (D4): re-processing the same file must delta-update the existing IPRs via the Upload Ledger identity index, not create duplicate rows (observed 18-Jul: duplicate extraction created 10 duplicate SKU rows; cleaned manually).

**D16 implementation note (18-Jul-2026):** site-root URLs get a bounded same-host crawl (≤6 best product-scored links, depth 1); crawled PDF/xlsx files enter triage individually; product-bearing pages combine into one catalogue source; JS-rendered sites that yield nothing produce an explanatory D11 verification entry (verified live against se.com) — never a silent no-op. AI-tier outages (e.g. exhausted API credits) route uploads to human triage with the reason shown instead of failing.
